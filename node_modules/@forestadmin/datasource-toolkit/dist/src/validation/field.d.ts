import { Collection } from '../interfaces/collection';
import { ColumnSchema, PrimitiveTypes } from '../interfaces/schema';
import { ValidationTypes } from './types';
export default class FieldValidator {
    static validate(collection: Collection, field: string, values?: unknown[]): void;
    static validateValue(field: string, schema: ColumnSchema, value: unknown, allowedTypes?: readonly (PrimitiveTypes | ValidationTypes)[]): void;
    static checkEnumValue(type: PrimitiveTypes | ValidationTypes, columnSchema: ColumnSchema, enumValue: unknown): void;
}
//# sourceMappingURL=field.d.ts.map