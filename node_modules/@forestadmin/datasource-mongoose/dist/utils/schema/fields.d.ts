import { CollectionSchema, RecordData } from '@forestadmin/datasource-toolkit';
import { Model } from 'mongoose';
/** Generate forest admin schema from mongoose schema */
export default class FieldsGenerator {
    static buildFieldsSchema(model: Model<RecordData>, prefix?: string, skips?: string[]): CollectionSchema['fields'];
    /** Fake primary key that will be used when we're flattening collections */
    private static buildVirtualPrimaryKey;
    private static buildManyToOne;
    /** Build ColumnSchema from CleanSchema */
    private static buildColumnSchema;
    /** Compute column type from CleanSchema */
    private static getColumnType;
    /** Build ColumnType from CleanSchema recursively */
    private static getColumnTypeRec;
    /** Get enum validator from field definition */
    private static getEnumValues;
}
//# sourceMappingURL=fields.d.ts.map